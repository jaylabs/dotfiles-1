---
- name: configure dropbox repo
  copy:
    src: dropbox.repo
    dest: /etc/yum.repos.d/dropbox.repo
    owner: root
    group: root
    mode: 0644

- name: install dropbox
  become: true
  package:
    name: dropbox
    state: present
