---
- hosts: all
  become: True
  remote_user: vagrant

  tasks:

    - name: update packages
      package:
        name: "*"
        state: latest

    - name: install initial packages
      package:
        name: "{{ item }}"
        state: present
      loop:
        - wget
        - nmap
        - curl
        - git
        - vim
